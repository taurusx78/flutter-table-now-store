import 'package:flutter/material.dart';

class DeleteStorePage extends StatelessWidget {
  const DeleteStorePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
