import 'package:flutter/material.dart';

class InsideInfoPage extends StatelessWidget {
  const InsideInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
